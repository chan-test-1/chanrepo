// 백엔드에서 자바스크립트를 시켜주는 프로그램 -> nodejs 
// 웹은 explorer 에서 자바스크립트를 제공하지만 백엔드는 설치를 해야 인식한다.

// express 라는 백엔드 라이브러리로 백엔드를 만들어볼꺼다.

// 프론트에서 뭔가를 요청하면 백엔드는 받아서 조회를 하든 뭘하든 해서 응답을 해주는 역할이다.

// 일단 나는 프로젝트 폴더 안에 chanrepo_be 로 폴더 하나 더 만들어서 백엔드 패키지 하나 더 만들었다.
// chanrepo_be vscode로 열어라 
